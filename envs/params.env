# ---------------- PAIRS SEARCH -----------------

EXCLUDED = BTCUSDT, ETHUSDT

UPDATE_TIME_HOURS = 1
# як часто оновлювати список робочих інструментів (в годинах)

TICKSIZE_FILTER = 0.6
# розмір тіку в %

ATR_FILTER = 0.3
# середній ATR на вікно пошуку (KLINES_LEN)

PAIRS_LIMIT = 50
# макс. кількість інструментів для аналізу (обережно з KLINES_LEN та 429 помилкою - уникай бану!)

# ----------------- SIZE SEARCH -----------------

DEPTH_LEN = 500
# базова висота стакану

MIN_DEPTH_LEN = 100
# мінімальне значення висоти depth до помилки

KLINES_LEN = 240
# вікно пошуку на графіку

MIN_KLINES_LEN = 60
# мінімальне значення запиту klines до помилки

C_ROOM = 15
# кімната зліва

D_ROOM = 3
# вікно зверху і знизу стакану

WIGGLE_ROOM_PERC = 0.005
# вікно допуску сайзу в стакані відносно рівня на графіку вгору і вниз

ATR_DIS = 4.0
# мультиплікатор відстані до сайзу в ATR

ABS_DIS = 120.9
# мультиплікатор відстані до сайзу %

SIZE_MPL = 2.0
# мультиплікатор максимального сайзу

VOL_MPL_CHART = 3.0
# мультиплікатор відносності об'єму

VOL_MPL_DEPTH = 4.0
# мультиплікатор відносності об'єму якщо перевіряємо тільки сайз
